# User-facing changelog for the CURRENT release — the single source of truth.
# Consumed by BOTH the Forgejo release notes (create-release-bundle.sh) and the
# Telegram announcement (publish-local-stable.sh -> publish_zapretkvn_stable.py),
# so both channels always say the same thing.
# Rules: 1-6 items, one per line; each <= 180 chars; total joined <= 700 chars;
# blank lines and lines starting with '#' are ignored. Edit per release before
# publishing (the worktree must be clean, so commit this with the release prep).
Исправлен запуск некоторых импортированных ключей и конфигов: DNS-резолвер маршрута теперь выставляется автоматически (совместимость с новыми ядрами sing-box)
